These chains were produced with the likelihood data as of commit 883fd34, i.e. the
commit that added these chain files — not the current `likelihood/Lya_data.json`,
`likelihood/Lya_likelihood.py`, and `likelihood/raw_data/*.txt`, which were changed
in commit 90702c4. These chains are not directly comparable to runs made after that
commit; they would need to be rerun with the updated data.
